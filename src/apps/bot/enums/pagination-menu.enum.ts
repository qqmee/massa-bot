export enum PaginationMenuEnum {
  Changelog = 'changelog',
  StatsCompany = 'stats-company',
  StatsCountry = 'stats-country',
  StatsFree = 'stats-free',
  StatsVersion = 'stats-version',
  StatsNetwork = 'stats-network',
}
